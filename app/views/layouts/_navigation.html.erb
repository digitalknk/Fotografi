<% if ENV["SITE_TITLE"].nil? %>
<%= link_to "My Photographs", root_path, :class => 'brand' %>
<% else %>
<%= link_to ENV["SITE_TITLE"], root_path, :class => 'brand' %>
<% end %>
<div class="nav-collapse collapse">
<ul class="nav">
	<% if current_user %>
		<li>
			<%= link_to 'Upload Photo', new_photo_path %>
		</li>
		<li>
			<%= link_to 'Edit User', edit_user_path(current_user) %>
		</li>	
		<li>
			<%= link_to "Log out", logout_path %>
		</li>
	<% else %>
		<li>
			<%= link_to "Log in", login_path  %>
		</li>
	<% end %>
</ul>
</div>